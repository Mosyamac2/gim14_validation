# GIM-14 Validation Suite — Python dependencies
#
# Install GIM-14 itself first:
#   pip install -e /path/to/GIM-GIM14
#
# Then install these:
#   pip install -r requirements.txt

numpy>=1.24
requests>=2.28
scipy>=1.10          # optional: needed for Q10 (correlated equilibrium LP)
